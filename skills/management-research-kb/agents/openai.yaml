interface:
  display_name: "Management Research KB"
  short_description: "Ground management research in local scholarly evidence"
  default_prompt: "Use $management-research-kb to build linked knowledge notes or support a management-research task with two evidence-checking agents."

dependencies:
  tools:
    - type: "mcp"
      value: "management-research-kb"
      description: "Access configured local PDFs, Obsidian notes, Zotero metadata, and manuscript context"

policy:
  allow_implicit_invocation: true
